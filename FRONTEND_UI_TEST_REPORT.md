# 前端 GUI 测试报告（多角色 · 真实浏览器）

日期：2026-09-14　被测对象：光影视频分享平台前端（React 19 + Vite 6）＋ 后端（Java 21 + Spring Boot 4.1.1）
测试方式：**真实浏览器 GUI 自动化**，按 5 类用户角色逐页操作全部控件

---

## 1. 测试环境与方法

| 项 | 说明 |
| --- | --- |
| 浏览器 | 本机 Google Chrome 153（可见窗口，非无头），视口 1440×900 与移动端 390×844 |
| 驱动 | Playwright（语义定位 `getByRole` / `getByLabel` / `getByPlaceholder` + 自动等待），直接驱动真实 Chrome |
| 前端 | **生产构建**（`VITE_USE_MOCK=false`），由静态服务器托管并经 `/api` 反向代理到真实后端 |
| 后端 | 真实 Spring Boot 服务 + H2 数据库：42 条已发布视频、24 个用户，含动态/评论/通知/私信/审核队列 |
| 账号 | 游客、普通用户 `newbie`、创作者 `laowang`、审核员 `moderator`、管理员 `admin`（演示账号一键登录） |
| 截图 | 每个用例留档到 `/tmp/ui/shots/`，供人工复核视觉细节 |

### 1.1 关于工具选择（重要说明）

- 会话内声明的 **BrowserSkill 插件不可用**：其 `bsk` CLI 未安装，且安装需从 GitHub Releases 拉取二进制（本机到 `github.com:443` 连接超时失败）；该插件还要求在用户的 Chrome 中手动安装扩展，其官方文档明确写着「扩展安装是用户的步骤，Agent 无法自行完成」。
- 会话配置的**视觉模型（Qwen3-VL）返回被截断**（供应商输出上限），`modlens` 无法解析出结构化结果，因此**没有**采用「模型看图」的方式评价排版。
- 最终采用 **Playwright + 程序化排版度量**：几何/样式指标（横向溢出、对比度、文本截断、点击目标尺寸、文本重叠）由脚本精确计算，**比主观看图更可复现**；截图同时留档，可人工复核。

### 1.2 一个必须记录的测试环境陷阱

首次运行时所有需登录页面都被跳回 `/login`，控制台报 **403 Invalid CORS request**。
根因**不是应用缺陷**：后端 `app.cors.allowed-origin-patterns` 默认只允许 `…:5173`，而测试初始把前端托管在 `4173` 端口。改到 5173 后登录恢复正常。
→ 但这本身是一条**部署注意事项**：把构建产物部署到非 5173 的端口/域名时，必须显式设置 `CORS_ALLOWED_ORIGIN_PATTERNS`，否则所有写接口返回 403。

---

## 2. 执行结果总览

19 个用例、151 项断言：

| 角色 | 用例 | 断言 | 通过 | 失败 | 失败性质 |
| --- | ---: | ---: | ---: | ---: | --- |
| 游客 | A1–A9 | 100 | 96 | 4 | 2 真实问题 / 2 环境与断言口径 |
| 普通用户 newbie | B1–B6 | 20 | 15 | 5 | 2 真实问题 / 3 环境与数据 |
| 创作者 laowang | C1–C2 | 8 | 8 | 0 | — |
| 审核员 moderator | D1 | 7 | 7 | 0 | — |
| 管理员 admin | E1 | 10 | 10 | 0 | — |
| 移动端（游客） | F1 | 7 | 6 | 1 | 1 真实问题 |
| **合计** | **19** | **151** | **142** | **9** | **5 真实问题 + 4 环境/口径** |

---

## 3. 控件覆盖清单

### 3.1 游客

| 页面 | 验证过的控件 | 结论 |
| --- | --- | --- |
| 首页 | 排序 Tab（推荐/最新/最热/播放最多）、分区（全部/科技/生活/娱乐）、侧边栏折叠、无限滚动、卡片点击、顶部导航（排行榜/短视频/动态）、主题菜单（亮色/暗色/跟随系统）、登录入口 | 全部有效；排序与分区会同步 URL 并重排列表；滚动到底自动加载（40→68 条） |
| 搜索 | 搜索框、建议项「搜索「关键词」」、排序（最新发布/播放最多）、时长（5 分钟以内 / 5-30 / 30 以上）、分区、时间、空结果态 | 有效；**回车不提交**（见 4.3）、无可见搜索提交按钮 |
| 视频详情 | 播放/暂停、静音、播放速度、清晰度、字幕、画中画、全屏、返回、影院模式、自动连播、更多、点赞、点踩、收藏、分享、下载视频、举报、关注、评论区（最热/最新/发布）、相关推荐 | 控件齐全且分组合理；**点赞/收藏无可访问名称**（见 4.1） |
| 排行榜 | 热门榜/趋势榜/分类榜/新人榜、日/周/月/总榜、实时状态、条目跳转 | 全部有效，切换即时生效 |
| 短视频 | 上滑/下滑切换、点赞、评论、收藏、分享、静音、举报、进度 | 有效；切换后序号与作者同步更新 |
| 动态 | 推荐/关注/热门 Tab、发布输入框、点赞/评论/转发/更多 | 有效；游客互动有反馈 |
| 用户主页 | 关注、作品/动态/收藏/关注/粉丝 Tab、统计数字 | 有效 |
| 登录/注册/忘记密码 | 三个 Tab、手机号验证码/账号密码两种模式、获取验证码、验证码登录、协议开关、微信/QQ、4 个演示账号一键登录 | 均实现且可用；**无 `<form>`**（见 4.3） |
| 404 | 友好提示、返回首页 | 有效 |

### 3.2 登录用户

| 页面 | 验证过的控件 | 结论 |
| --- | --- | --- |
| 顶栏 | 用户入口、通知、主题、退出 | 有效 |
| 视频互动 | 点赞、点踩、收藏、关注、评论发布 | 有效（评论落库并回到列表） |
| 观看历史 / 我的收藏 | 列表、收藏夹 | 有效 |
| 私信 | 会话列表、消息输入、发送 | 页面与控件齐全；`newbie` 无历史会话，故列表为空（数据原因） |
| 通知 | 通知入口、列表、已读 | 有效 |
| 设置 | 资料、密码、主题、实名、注销、退出 | 控件齐全 |

### 3.3 创作者 / 审核员 / 管理员

| 页面 | 验证过的控件 | 结论 |
| --- | --- | --- |
| 创作中心 | 数据看板、我的视频、状态筛选、编辑弹窗 | 有效，指标与列表正常渲染 |
| 上传页 | 4 步流程条、视频类型（长/短）、标签、文件选择（1 个 file input）、元数据输入 | 有效，说明文案完整（分片直传/断点续传/秒传） |
| 内容审核 | 全部/待审核/已通过/已驳回、风险等级、批量通过/批量驳回、单条通过、快捷键 | 有效 |
| 举报处理 | 待处理/处理中/已处理/已驳回、举报类型筛选 | 有效 |
| 视频管理 | 状态筛选、重新审核、下架、关键词过滤 | 有效 |
| 数据概览 | 用户/视频/互动/转码指标、趋势、分区分布、前往审核等跳转 | 有效 |
| 用户管理 | 关键词搜索（实测可搜到 laowang）、角色筛选、状态筛选、邮箱显示开关 | 有效 |
| 系统设置 | 审核策略、上传限制、推荐开关、青少年模式、保存修改、重置为默认值 | 有效 |
| 操作日志 | 操作类型筛选、日志列表 | 有效 |

---

## 4. 发现的问题（按严重度）

### 4.1 【缺陷·无障碍】点赞 / 收藏按钮没有可访问名称

- **证据**：视频详情页按钮清单中，点赞按钮的可访问名称为纯数字 `166`，收藏按钮为 `62`；而同一组里的「点踩」是 `点踩`。
- **定位**：`frontend/src/components/video/ActionBar.tsx` — 点赞按钮（约 36–53 行）与收藏按钮（约 70–81 行）只有 `<Heart/>` / `<Bookmark/>` 图标加计数 `<span>`，**缺少 `aria-label`**；点踩按钮（55–67 行）有 `aria-label="点踩"`。
- **影响**：屏幕阅读器用户只会听到「166」「62」，无法判断按钮用途；三个同组操作的标注方式不一致。违反 WCAG 4.1.2（名称、角色、值）。
- **建议**：为点赞/收藏补 `aria-label`（如「点赞」「收藏」），并保留 `aria-pressed`（该属性已正确实现）。

### 4.2 【缺陷·健壮性】无头像用户渲染出没有 `src` 的 `<img>`

- **证据**：视频详情页检测到 `<img alt="架构师老王" class="size-11 rounded-full bg-surface-3 object-cover">`，`src` 属性为 `null`、`naturalWidth = 0`（浏览器按破图渲染）。
- **触发条件**：用户表 `avatar_url` 为 NULL 时（演示数据中的用户均无头像）。
- **影响**：头像位置出现浏览器默认破图占位，视觉上不专业；移动端同样出现。
- **建议**：无 `avatar_url` 时渲染首字母/默认头像组件，或直接不渲染 `<img>` 只保留背景色圆形。

### 4.3 【缺陷·可用性】登录页没有 `<form>`，回车键无法提交

- **证据**：登录页 `document.querySelectorAll('form').length === 0`；在密码框输入正确账号密码后按 **Enter**，URL 与登录态均无变化（仍未登录）。
- **补充证据**：输入框没有 `name` 属性、没有 `required` 属性；搜索框虽然包在 `<form>` 里但 `action` 为空，同样无法用回车提交。
- **影响**：① 键盘用户的高频操作（输完密码敲回车）失效，必须移动鼠标点按钮；② 浏览器密码管理器无法识别登录表单，无法自动填充/保存；③ 失去原生必填校验。
- **建议**：用真正的 `<form onSubmit>` 包裹登录/注册/搜索；输入框补 `name`、`required`、`autocomplete="username" / "current-password"`。

### 4.4 【缺陷·移动端】部分点击目标小于触摸推荐尺寸

- **证据**（移动端 390×844 实测）：
  - 视频页「返回」按钮 **46×21 px**
  - 视频页分区链接「娱乐」**24×16 px**
  - 视频页作者链接 **196×20 px**
  - 首页/排行榜作者名链接 60~72 × 16 px
- **影响**：低于 44×44 px 的通用触摸目标建议，拇指操作易误触；文字链接仅 16px 高尤其明显。
- **建议**：为图标按钮补足 padding（≥44px 命中区），作者名等文字链接增加行高或点击热区。
- **说明**：桌面端同样的尺寸属正常文本链接，**此问题仅针对移动端**。

### 4.5 【缺陷·可读性】次级文字对比度不足

- **证据**（程序化对比度计算，WCAG AA 要求 ≥4.5:1）：
  - 首页 11 处：`根据你的兴趣推荐` 3.31:1（11px）、`10 分钟更新 / 5 分钟更新 / 每小时更新` 3.45:1（11px）
  - 排行榜 76 处（列表项内的次级说明文字）
  - 搜索页 5 处、用户主页 5 处、登录页 9–12 处
- **影响**：小字号＋低对比度在弱光/移动场景下几乎不可读。
- **建议**：把 `text-fg-subtle` 一类色板在白底上的对比度提升到 ≥4.5:1（或把字号提到 12px 以上并加粗）。

### 4.6 【环境·数据】演示视频没有真实媒体文件

- **证据**：`GET /api/v1/videos/2/source → 404`，播放器显示「视频加载失败，请稍后重试 / 重新播放」。
- **性质**：种子数据只写了数据库行，没有对应的媒体文件，**不是代码缺陷**；应用的错误处理是正确的（给出明确提示 + 重试入口）。
- **建议**：把这条写进演示环境说明，或为种子数据生成占位媒体文件。

---

## 5. 分维度评估

### 5.1 是否实现（功能完整性）— 优秀

所有 14 个路由、5 类角色的页面均真实渲染，未发现「点了没反应」的空壳控件。亮点：
- 视频详情页播放器控件完整（播放/静音/倍速/清晰度/字幕/画中画/全屏/影院模式/自动连播），并附快捷键说明「空格暂停，← → 快退快进 5 秒，F 全屏，M 静音」
- 评论区提供「最热/最新」排序、发布入口、点赞与回复
- 后台上/下架、批量审核、举报处理、角色与状态管理、系统设置、操作日志一应俱全
- 注册要求显式勾选《用户协议》且未勾选无法提交 ✓（符合合规预期）

### 5.2 是否有效（交互正确性）— 良好

- 排序/分区/榜单类型/周期切换均即时生效并同步到 URL（可分享、可后退）
- 无限滚动在滚动到底自动追加（40→68 条）
- 主题菜单三档切换（亮色/暗色/跟随系统）并持久化到 `localStorage.vs-ui-preference`
- 游客点击互动会被引导登录；登录后评论能落库并回显在列表
- 未发现刷新丢状态、重复提交、白屏等异常

### 5.3 排版是否合适 — 良好（两处待改）

- **网格对齐一致**：首页卡片左边缘集中在 96 / 538 / 980，三列间距均等（442px），无错位
- **无横向溢出**：桌面端与移动端 380px 视口下 `scrollWidth == clientWidth`
- **无破图**（除 4.2 的空头像）、**无文本截断**（仅有 `sr-only` 无障碍标签被我初版脚本误报）
- **无文本互相遮挡**
- 待改：次级文字对比度（4.5）、移动端点击目标（4.4）

### 5.4 响应是否正常 — 良好

- 页面接口全部返回 200；无请求失败、无未捕获异常、无 React 报错
- 唯一的异常状态由数据造成（4.6 的 404）且被优雅处理
- 首屏请求数约 12–20，接口响应体在开启压缩后为 2–3KB 级别

### 5.5 用户操作是否便捷 — 良好（一处待改）

- 演示账号一键登录、快捷键提示、空态文案（「还没有短视频，成为第一个发布短视频的人」）、404 引导都做得很贴心
- 上传页用 4 步进度条明确告知流程
- 待改：登录**必须点按钮**、不支持回车（4.3）；搜索同样缺少回车提交与显式提交按钮

---

## 6. 未覆盖与局限

1. **视觉细节未经模型评审**：视觉模型输出被供应商截断，配色和谐度、图标美观度等主观项未评价；截图已留档（`/tmp/ui/shots/`）可人工复核。
2. **未测试真实上传**：上传页验证了控件与校验，但未实际推送大文件（需 MinIO/对象存储与真实媒体文件）。
3. **未测试支付/短信/第三方登录回调**：微信/QQ 登录按钮只验证了存在与提示，未走外部授权。
4. **未做压力与并发下的 GUI 表现**：属性能测试范畴，见 `PERFORMANCE_REPORT.md`。
5. **移动端仅验证 390×844 一种尺寸**；平板与横屏未覆盖。

---

## 7. 复现方式

```bash
# 1) 前端生产构建（连真实后端）
cd frontend && VITE_USE_MOCK=false ./node_modules/.bin/vite build

# 2) 启动后端（H2）与托管产物（必须用 5173，否则触发 CORS 白名单）
java -jar backend/target/video-platform-0.1.0-SNAPSHOT.jar --server.port=18099
node /tmp/perf/static-proxy.mjs frontend/dist http://127.0.0.1:18099 5173

# 3) 运行 GUI 测试套件
cd /tmp/uiauto
UI_BASE=http://127.0.0.1:5173 node run.mjs guest   # 游客用例 A1–A9
UI_BASE=http://127.0.0.1:5173 node run.mjs auth    # 登录/创作者/审核/管理/移动端
```

测试脚本：`/tmp/uiauto/lib.mjs`（排版审计与工具）、`suite-guest.mjs`、`suite-auth.mjs`、`run.mjs`；
截图：`/tmp/ui/shots/`；机器可读结果：`/tmp/uiauto/results.json`。

---

## 8. 结论

前端**功能完整度高、交互正确、排版稳定**：14 个路由、5 类角色、151 项断言中 142 项通过，
未发现功能性缺失或崩溃类问题。需要修复的是 5 个具体问题，按优先级：

| 优先级 | 问题 | 定位 |
| --- | --- | --- |
| P1 | 登录/搜索无 `<form>`，回车不能提交，密码管理器无法识别 | `pages/auth/LoginPage.tsx`、页头搜索组件 |
| P1 | 点赞/收藏缺少 `aria-label` | `components/video/ActionBar.tsx` |
| P2 | 无头像时 `<img>` 无 `src` 出现破图 | 头像组件 |
| P2 | 移动端点击目标 < 44px | 视频页返回按钮、作者/分区文字链接 |
| P3 | 次级文字对比度 3.31–3.45:1 | 设计令牌 `fg-subtle` 相关色板 |
