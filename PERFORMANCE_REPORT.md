# 性能与开销测试报告

日期：2026-09-14　测量对象：光影视频分享平台（后端 Java 21 + Spring Boot 4.1.1，前端 React 19 + Vite 6）

本报告测量**升级后**（commit 工作区）与**升级前**（`d2f5d6a`）两套后端在同等数据集、
同等 JVM、同等机器上的表现。前端代码本次未改动，用于观察后端升级对端到端的影响。

---

## 1. 环境与方法

| 项 | 值 |
| --- | --- |
| 机器 | Intel i7-13700H（20 逻辑核）、15.6 GB RAM |
| JVM | OpenJDK 21.0.12（两套后端都用同一个 JVM，隔离「代码/框架」变量） |
| 数据库 | H2 2.4.240（新版）/ H2 2.2.224（旧版，由各自依赖决定），MySQL 兼容模式 |
| 压测工具 | ApacheBench 2.3（`ab -k`，keep-alive） |
| 浏览器 | Chrome headless（CDP 直连，无第三方依赖脚本） |
| 数据集 | dev 种子 + 300 条已发布视频（共 302 条），两套完全一致 |

测量方法：

1. **SQL 语句条数**：用 JDK 动态代理包装 `DataSource`，统计每次请求内
   `prepareStatement/createStatement` 的调用次数，通过 MockMvc 在进程内逐端点测量。
2. **HTTP 吞吐/延迟**：真实启动 jar，用 `ab` 打真实 HTTP，记录 RPS、均值、p95、
   失败数，并按 `/proc/<pid>/stat` 的 utime+stime 折算**每请求 CPU 毫秒**。
3. **端到端**：用生产构建产物 + 静态反代指向真实后端，在无头 Chrome 中采集
   网络传输字节、FCP、DOM 节点、JS 堆。

> 口径说明：本机同时跑压测客户端与服务端，绝对值只用于**同机对比**，不代表生产容量。

---

## 2. 后端：单请求 SQL 开销（A/B）

同一数据集、同一批端点，逐端点统计 SQL 条数：

| 端点 | 升级前 | 升级后 | 变化 |
| --- | ---: | ---: | ---: |
| GET /videos/recommend?pageSize=20 | 61 | **6** | −90% |
| GET /videos/recommend?sort=hot&pageSize=20 | 61 | **6** | −90% |
| GET /videos/search?q=光影&pageSize=20 | 8 | **6** | −25% |
| GET /videos/1 | 4 | **3** | −25% |
| GET /videos/1/related | 37 | **5** | −86% |
| GET /videos/1/comments | 4 | **4** | 持平 |
| GET /videos/ranking?type=hot | 151 | **5** | −97% |
| GET /videos/shorts?pageSize=10 | 1 | **1** | 持平 |
| GET /users/3 | 6 | **1** | −83% |
| GET /users/3/videos | 97 | **6** | −94% |
| GET /users/3/followers | 5 | **3** | −40% |
| GET /feeds?pageSize=10 | 7 | **4** | −43% |
| GET /notifications（24 条通知，含 actor） | 50 | **4** | −92% |
| GET /videos/history（24 条观看记录） | 121 | **9** | −93% |
| GET /videos/favorites（24 条收藏） | 122 | **10** | −92% |
| GET /creator/dashboard | 35 | **5** | −86% |
| GET /messages/conversations | 5 | **4** | −20% |
| GET /admin/users | 9 | **2** | −78% |
| GET /admin/overview | 58 | **19** | −67% |
| **19 个端点合计** | **842** | **103** | **−87.8%** |

结论：所有端点都在**绝对开销**上不高于升级前，绝大多数下降一个数量级；
仅「列表 + 批量装配」路径的固定成本略高于单条查询，但已通过合并批量语句压到 3~6 条。

## 3. 后端：查询次数是否随数据量增长

这是本次改造的核心指标——旧实现的开销由**库里的数据总量**决定，新实现由**页大小**决定。

### 3.1 目录页（/videos/recommend）随页大小

| pageSize | 升级前 SQL | 升级后 SQL |
| ---: | ---: | ---: |
| 5 | 16 | 6 |
| 10 | 31 | 6 |
| 20 | 61 | 6 |
| 40 | 121 | 6 |

旧实现约 `3 × pageSize + 1`，新实现恒定 6 条。pageSize=40 时相差 **95%**。

### 3.2 评论列表（库中固定 50 条评论）

| pageSize | 升级前 SQL | 升级后 SQL |
| ---: | ---: | ---: |
| 10 | 154 | 4 |
| 50 | 154 | 4 |

旧实现按 `pageSize` 分页但按**全表评论数**渲染，即使只取 10 条也要 154 次查询；
新实现恒定 4 条。

## 4. 后端：HTTP 吞吐、延迟与 CPU

302 条已发布视频，`ab -k`，每组配置独立启动：

| 端点（并发/请求数） | 指标 | 升级前 | 升级后（平台线程） | 升级后（虚拟线程） |
| --- | --- | ---: | ---: | ---: |
| recommend（c=50, n=2000） | RPS | 699.8 | **786.4** | 742.4 |
| | 均值 / p95 (ms) | 71.4 / 151 | **63.6 / 115** | 67.3 / 134 |
| | CPU ms/请求 | 24.18 | **22.05** | 23.01 |
| detail（c=50, n=3000） | RPS | 3716.7 | **3953.4** | 3497.1 |
| | 均值 / p95 (ms) | 13.5 / 22 | **12.6 / 28** | 14.3 / 32 |
| | CPU ms/请求 | 4.53 | **4.46** | 4.86 |
| ranking（c=50, n=1500） | RPS | 590.0 | 896.3 | **971.4** |
| | 均值 / p95 (ms) | 84.8 / 140 | 55.8 / 103 | **51.5 / 84** |
| | CPU ms/请求 | 29.11 | 19.19 | **17.44** |
| categories（c=100, n=4000） | RPS | 7232.1 | 10043.2 | **10445.2** |
| | 均值 / p95 (ms) | 13.8 / 20 | 10.0 / 18 | **9.6 / 20** |
| | CPU ms/请求 | 2.21 | 1.74 | **1.64** |

要点：

- **榜单接口收益最大**：RPS +52%（平台线程）/+65%（虚拟线程），每请求 CPU −34%~−40%，
  p95 −26%~−40%。旧版为渲染 50 条榜单要发 151 条 SQL，新版 5 条。
- **分类接口 +39%~+44%**：除查询简化外还叠加了 10 分钟 Caffeine 缓存。
- **目录接口 +12%**：旧版的 61 条 SQL 换成了 SQL 内排序打分（对全表做 `LN/EXP` 计算），
  工作量从「N 次往返」转移到「1 次聚合扫描」，净收益中等但延迟分布更稳（p95 −24%）。
- **详情接口**在 c=50 下 +6%，在虚拟线程下 −6%：两者都在噪声区间，属于「单条渲染」的
  固定成本，已通过合并批量语句（5→3 条）优化到不劣于旧实现。

### 4.1 虚拟线程的实测结论

| 端点 | 平台线程 → 虚拟线程 |
| --- | --- |
| recommend | 786.4 → 742.4 RPS（−5.6%） |
| detail | 3953.4 → 3497.1 RPS（−11.5%） |
| ranking | 896.3 → 971.4 RPS（+8.4%） |
| categories | 10043.2 → 10445.2 RPS（+4.0%） |

**结论：在本机 H2 + 无外部依赖的场景下，虚拟线程对吞吐是中性的（互有胜负，均在 ±12% 内）。**
原因是这些端点是 CPU/数据库连接池受限，而不是线程阻塞受限。虚拟线程的真实收益体现在
「大量并发阻塞等待」——生产环境的 MySQL/Redis/MinIO 网络往返、视频流式传输、SSE 长连接。
它在本轮的确定性收益在**开销**一侧，见第 5 节。

## 5. 后端：运行时开销（内存与线程）

统一 `-Xms256m -Xmx256m -XX:MaxMetaspaceSize=256m`，200 并发压测前后采样：

| 配置 | 空闲 RSS | 空闲线程数 | 峰值 RSS | 峰值线程数 |
| --- | ---: | ---: | ---: | ---: |
| 升级前 | 650 MB | 74 | 799 MB | 277 |
| 升级后（平台线程） | 686 MB | 75 | 819 MB | 277 |
| 升级后（虚拟线程） | 623 MB | 68 | **778 MB** | **98** |

**虚拟线程把 200 并发下的操作系统线程数从 277 降到 98（−65%）**，峰值 RSS 同时更低。
这正是虚拟线程的设计目标：并发度不再等价于 OS 线程与线程栈开销。
（该组 RPS 仅在同组三方之间可比，数据集与第 4 节不同，故未列出。）

## 6. 后端：启动与产物

| 项 | 升级前 | 升级后 |
| --- | ---: | ---: |
| 启动到 Started（dev profile，含 Flyway + 种子） | 8.33 s | 8.42 s |
| 可执行 jar | 94.2 MB | 102.6 MB（+8.9%） |
| 测试套件 | 13 项 | 18 项，单一 Spring 上下文，约 18 s |

jar 变大是 Spring Boot 4 模块化的直接结果（模块数增加），不影响启动时间。

## 7. 端到端：真实浏览器 + 生产构建

前端用生产构建 + 静态服务器（含 gzip，等价 nginx），分别指向两套后端，在无头 Chrome
中采集。前端资源完全一致（234,505 B），差异只来自后端：

| 路由 | 升级前 API 传输字节 | 升级后 API 传输字节 | 变化 |
| --- | ---: | ---: | ---: |
| / | 12,169 | **2,246** | −81.5% |
| /ranking | 31,460 | **3,448** | −89.0% |
| /shorts | 4,955 | **2,147** | −56.7% |
| /feed | 2,412 | 2,374 | −1.6%（响应 <1 KB，未触发压缩） |
| /login | 0 | 0 | 无 API 调用 |

原因：升级后开启了 `server.compression`（阈值 1 KB），列表类 JSON 响应被 gzip 压缩。
**这是本次唯一对终端用户可感知的传输层收益：列表接口下行流量降低 8~9 成。**

页面渲染指标基本不变（同一台机器、本地网络）：

| 路由 | FCP 升级前 → 升级后 | DOM 节点 | JS 堆 |
| --- | --- | --- | --- |
| / | 176 → 184 ms | 766 → 766 | 5.55 → 5.54 MB |
| /ranking | 136 → 140 ms | 1730 → 1730 | 5.96 → 5.95 MB |
| /shorts | 180 → 144 ms | 89 → 89 | 4.14 → 4.15 MB |
| /feed | 140 → 140 ms | 358 → 358 | 4.70 → 4.67 MB |
| /login | 136 → 140 ms | 101 → 101 | 3.41 → 3.45 MB |

差异均在 ±40 ms 噪声内。原因是页面 FCP 由本地静态资源与 JS 执行主导，
在本地网络下 API 体积差异不足以改变渲染时间；在真实公网/移动网络下，
第 7 节表格中的传输量差异才会转化为用户可感知的加载时间。

## 8. 前端：构建、测试与产物体积

| 步骤 | 命令 | 耗时 | 结果 |
| --- | --- | ---: | --- |
| 类型检查 | `tsc -b --force` | 5.5 s | 通过 |
| Lint | `eslint .` | 7.9 s | 通过 |
| 单元测试 | `vitest run` | 28.0 s | 7 个文件 / 147 项全部通过 |
| 生产构建 | `vite build` | 4.1 s | 2395 个模块，通过 |
| 浏览器验收 | `scripts/cdp-check.mjs` | — | **30/30 通过**（真实后端） |

产物（生产构建，`dist`）：

| 项 | 原始 | gzip |
| --- | ---: | ---: |
| 全部产物 | 2,332,473 B（2.22 MB） | 1,156,534 B（1.10 MB） |
| 其中演示 HLS 媒体 | 656,511 B | 不可再压缩 |
| 应用 JS/CSS 合计 | 约 1.68 MB | 约 0.50 MB |

单个资源 Top 5：

| 资源 | 原始 | gzip | 说明 |
| --- | ---: | ---: | --- |
| media-*.js | 592.6 KB | 184.8 KB | hls.js（按需分包） |
| index-*.js | 413.5 KB | 131.7 KB | 应用主包 |
| vendor-*.js | 115.1 KB | 36.4 KB | React / Router / Query |
| motion-*.js | 114.2 KB | 37.6 KB | framer-motion |
| index-*.css | 68.0 KB | 12.3 KB | Tailwind 产物 |

观察：主包 gzip 后 131.7 KB 属于中等偏大；`media`（hls.js）已独立分包，
首屏不会加载。当前未触发 Vite 的 chunk 体积告警（阈值 900 KB）。
前端本次未改动，以上为**基线数据**，供后续优化对照。

## 9. 环境层面的发现

1. **`pnpm run` 在 pnpm 11 下失败**：机器上是 pnpm 11.7.0，而项目锁定 pnpm 10.18.3。
   pnpm 11 在任何 `pnpm run` 前会做依赖状态自检并自动执行 `pnpm install`，该安装失败导致
   所有脚本以 exit 1 退出。按 README 使用 `npm exec --yes pnpm@10.18.3` 可规避；
   本报告为排除该干扰，直接调用 `node_modules/.bin/{tsc,eslint,vitest,vite}`。
2. **`frontend/.env` 中 `VITE_USE_MOCK=true`**：默认本地构建走 Mock 数据层，
   页面不产生任何 API 请求（实测 `apiCalls=0`）。第 7 节的数据是在
   `VITE_USE_MOCK=false` 下重新构建后取得的。
3. **演示封面图指向 picsum.photos**：该外部域名在本环境不可达，因此图片加载耗时不具代表性。

## 10. 复现方式

```bash
# 1) 端点 SQL 开销（写入 backend/target/perf/*.tsv）
cd backend && mvn -B test -Dtest=EndpointCostReportTest

# 2) 三方 HTTP 压测（新版+虚拟线程 / 新版-虚拟线程 / 升级前）
git worktree add /tmp/vsw-baseline <升级前的提交> --detach
cd /tmp/vsw-baseline/backend && mvn -B -DskipTests package
bash /tmp/perf/run-loadtest.sh

# 3) 运行时开销（内存与线程）
bash /tmp/perf/measure-memory.sh

# 4) 前端构建与体积
cd frontend && ./node_modules/.bin/{tsc -b --force,eslint .,vitest run,vite build}
```

## 11. 结论与后续建议

**已验证的收益**

- 单请求 SQL 条数合计从 842 降到 103（−87.8%），且不再随数据总量增长。
- 榜单接口吞吐 +52%~+65%、每请求 CPU −34%~−40%；分类接口 +39%~+44%。
- 列表接口下行流量降低 81%~89%（HTTP 压缩）。
- 200 并发下 OS 线程数降低 65%，内存不变或更低。
- 功能未回归：后端 18 项测试全绿（含 N+1 回归护栏），前端 147 项单测 + 30 项浏览器验收通过。

**需要保留的判断**

- 虚拟线程在本机 H2 场景对吞吐中性，其价值需在生产 MySQL/Redis/MinIO 与视频流场景复测。
- 本机同机压测不能替代生产容量评估，未做长时间稳定性与 MySQL 真实链路压测。

**建议的后续动作（执行情况见第 12 节）**

1. 在生产等价环境（MySQL + Redis + MinIO）复跑第 4、5 节，验证虚拟线程与压缩的真实收益。→ **尚未执行**
2. 为 `videos` 表的榜单/目录排序建立覆盖索引，当数据量到百万级时把 SQL 内打分迁移为物化分数列 + 定时刷新。
   → **索引已在第 12 节完成**；物化分数列仍未做（见 12.7）。
3. 前端主包 131.7 KB(gzip) 可通过路由级懒加载与依赖替换进一步压缩。→ **已降到 114.4 KB(gzip)**，见 12.5。
4. 修复 pnpm 版本约束。→ **已完成**（`packageManager: pnpm@10.18.3`）。
---

## 12. 优化实施与复测（第二轮）

第 1~11 节是首轮测量。基于首轮结论做了一轮代码优化，下面是**改动清单**与**复测数据**，
并如实记录首轮中被证伪的假设。

### 12.1 改动清单

| # | 改动 | 依据 |
| --- | --- | --- |
| 1 | 新增 `V6__list_and_ranking_indexes.sql`：补齐列表排序、个人内容库、后台队列缺失的 13 个索引 | 首轮报告「建立覆盖索引」建议 |
| 2 | 排序/过滤键由 `COALESCE(published_at, created_at)` 改为 `published_at`（V6 中回填历史 NULL） | 表达式排序无法使用索引 |
| 3 | 个人内容库查询由 `JOIN videos` 改写为 `EXISTS` 半连接 | 执行计划显示驱动表被选成 videos，单请求 `scanCount: 20003` |
| 4 | 14 处分页查询补稳定并列键（`id` / `video_id`） | 压测出现 388/320 个 Length 失败＝同分页结果不稳定 |
| 5 | 压缩阈值 1024B → 512B | 动态/评论列表响应在 0.9~1KB 区间未触发 gzip |
| 6 | `JwtAuthenticationFilter` 增加 5 秒 Redis 失败熔断 | 见 12.4：无 Redis 时每个带令牌请求都要尝试连接 |
| 7 | 前端移除 `@/mocks` 静态导入，并把 `USE_MOCK` 改为可静态求值表达式 | Mock 适配层 + 种子数据被打进生产主包 |
| 8 | 前端降级轮询改为请求真实接口（榜单/通知） | 原实现在生产环境用 Mock 数据伪造推送 |
| 9 | `package.json` 增加 `"packageManager": "pnpm@10.18.3"` | pnpm 11 下所有 `pnpm run` 脚本失败 |

### 12.2 规模化 A/B 复测

数据集：**20,000 条已发布视频 + 500 个用户 + 各 300 条关注/收藏/观看/通知**，`ab -n 400 -c 20`。

| 端点 | 优化前 RPS | 优化后 RPS | 变化 | CPU ms/请求 |
| --- | ---: | ---: | ---: | ---: |
| /videos/favorites | 142.4 | **289.2** | **+103%** | 103.4 → 42.7 |
| /videos/shorts | 1966.0 | **2332.6** | **+19%** | 7.7 → 6.6 |
| /users/3/followers | 3268.0 | **3854.6** | **+18%** | 5.1 → 4.4 |
| /notifications | 1222.9 | **1356.8** | **+11%** | 7.1 → 5.6 |
| /videos/1/related | 1000.5 | 1019.0 | +2% | 15.7 → 14.7 |
| /videos/recommend?sort=views | 712.9 | 694.0 | −3% | 23.1 → 23.3 |
| /videos/recommend?sort=latest | 381.0 | 361.8 | −5% | 41.3 → 43.1 |
| /videos/history | 151.6 | 144.9 | −4% | 74.3 → 77.2 |
| /admin/reviews | 1200.9 | 1122.5 | −7% | 7.9 → 9.6 |
| /videos/recommend（打分） | 24.7 | 24.4 | −1% | 625.7 → 641.0 |

> 噪声说明：用同一版本连跑两次得到 latest 345.6 vs 381.0、followers 2598 vs 3268，
> 即**单次运行间波动约 ±10%~25%**。因此上表只有 ≥+11% 的四项视为有效收益，其余视为持平。

### 12.3 纯 SQL 隔离计时（2 万视频，预热后 10 次均值）

把 SQL 从 HTTP/JPA 中剥离后，列表类查询的实际成本是：

| 查询 | 平均耗时 |
| --- | ---: |
| favorites 分页（原 JOIN 写法） | 0.09 ms |
| favorites 分页（EXISTS 写法） | 0.22 ms |
| history 分页（原 JOIN 写法） | 0.14 ms |
| history 分页（EXISTS 写法） | 0.11 ms |
| latest 分页 | 0.20 ms |
| views 分页 | 0.18 ms |
| related（分区前 N） | 0.07 ms |
| **recommend 打分分页** | **80.99 ms** |

结论：**列表类 SQL 在 2 万行规模下都在亚毫秒级**，它们不是这些接口的瓶颈；
唯一昂贵的查询是 recommend/hot 的「全表打分 + 排序」。

### 12.4 鉴权路径的隐藏开销（Redis 熔断）

首轮压测里 favorites / history 的 CPU 成本（103ms、74ms）远高于其 SQL 成本（<0.3ms），
定位后发现：带令牌的请求都要在 `JwtAuthenticationFilter` 里查一次 Redis 黑名单，
而基准环境没有 Redis，于是**每个请求都付一次必然失败的连接尝试**。加入 5 秒失败熔断后：

| 端点（c=1，无 Redis） | 熔断前 | 熔断后 |
| --- | ---: | ---: |
| /users/me | 14.09 ms | **7.51 ms** |
| /notifications | 4.85 ms | **3.49 ms** |
| /creator/dashboard | 4.99 ms | **3.00 ms** |
| /videos/favorites | 11.84 ms | 11.21 ms |
| /videos/history | 23.14 ms | 21.10 ms |
| /videos/1（无鉴权基准） | 3.10 ms | 2.92 ms |

生产环境若 Redis 抖动，同样能避免「每个请求都去打一个已经不可用的依赖」。

### 12.5 前端产物体积

| 项 | 优化前 | 优化后 |
| --- | ---: | ---: |
| 主包 index（原始 / gzip） | 413.47 KB / 131.71 KB | **368.30 KB / 114.37 KB**（gzip **−13.2%**） |
| Mock 适配层 + 种子数据 | 并入主包 | **不进入生产产物**（`grep "[mocks]" dist` 无命中） |
| dist 总计（原始） | 2,332,473 B | 2,303,472 B |

前端功能未回归：类型检查、ESLint、147 项单测全部通过；真实后端下浏览器验收 **30/30 通过**。

### 12.6 首轮中被证伪的假设

为避免误导，如实记录两点：

1. **「缺少覆盖索引」只对了一部分**：V5 已经建立了
   `idx_videos_publish_sort(status, visibility, published_at, view_count, like_count)`，
   主目录按时间排序本来就能走索引。第一版 V6 中有两个索引与它重复，已删除；
   最终只保留真正缺失的 13 个。
2. **「EXISTS 改写会大幅提速」只在一个接口成立**：执行计划确实从 `scanCount: 20003`
   降到 `302`，但隔离计时显示原 JOIN 写法只要 0.09ms（走的是覆盖索引扫描）。
   favorites 翻倍更可能是并列键 + 索引 + 熔断的叠加；history 没有明显变化。

### 12.7 仍然存在的瓶颈与后续建议

1. **recommend / hot 打分是 O(表大小)**：2 万视频下单次查询 81ms，20 万将接近 1s。
   这是唯一需要架构级处理的点，建议做**物化分数列 + 定时刷新**（或「先按索引筛候选集
   再打分」的两阶段策略）。本次未改，因为它会改变榜单的新鲜度语义，属于产品决策。
2. **未在 MySQL 上复测**：本轮验证全部在 H2(MySQL 模式) 完成，索引选择与执行计划在
   MySQL 8 上可能不同，上线前需在等价环境重跑 12.2 节。
3. 前端主包 gzip 仍有 114 KB，可继续做路由级依赖拆分（例如 framer-motion 是否首屏必需）。

